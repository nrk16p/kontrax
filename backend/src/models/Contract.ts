// This file previously contained a Mongoose schema.
// The project now uses Firebase Firestore — all types are in types.ts.
// This file is kept only so any old imports don't break during transition.
export * from "./types"
